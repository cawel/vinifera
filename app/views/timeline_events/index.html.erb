<div style="margin-bottom: 30px;">
  <h1>Bienvenue sur Le-Tastevin !</h1>
  <table>
    <tr>
      <td>
        <% images = ["splash/wine_jaguar_80.png", "splash/wine_bottles_80.png", "splash/3_bottles_80.png", "splash/corks_80.png"].sort_by { rand } %>
        <%= image_tag images[0], :class => 'framed splash' %>
        <%= image_tag images[1], :class => 'framed splash' %><br/>
        <%= image_tag images[2], :class => 'framed splash' %>
        <%= image_tag images[3], :class => 'framed splash' %>
      </td>
      <td>
        <ul>
          <li>Consultez les critiques d'un vin</li>
          <li>Ecrivez une critique pour un vin</li>
          <li>Conservez vos critiques dans votre journal de critiques</li>
          <li>Souvenez-vous des vins que vous souhaitez deguster</li>
        </ul>
    </td></tr>
</table>
</div>

<h2 class="page_title">
  <%= link_to image_tag("/images/icons/feed.png"), activity_feed_url, :title => "Activit&eacute; sur le site " %>&nbsp; Activit&eacute; sur le site
</h2>

<table class="activity_stream">
  <tbody>
    <% @timeline_events.each do |event| %>
      <% wine = case event.event_type 
                when "reviewed" 
                  event.subject.wine
                when "edited" 
                  event.subject.wine
                when "cellar_wine_created" 
                  event.subject
                end %>
      <tr>
        <td class="wine">
          <span class="bottle">
            <%= link_to image_tag( glass_image_filename(wine), :alt => wine.name), wine_reviews_url(wine), :title => wine.name %>
          </span>
          <%= link_to wine.name, wine_reviews_url(wine), :title => wine.name, :class => "name" %>
          <% if event.event_type != 'cellar_wine_created' %>
            <span class="rating">
              <%= stars_rating event.subject.rating %>
            </span>
          <% end %>
          <small>
            <% case event.event_type when "reviewed" %>
              Nouvelle critique de
            <% when "edited" %>
              Critique modifi&#233;e par
            <% when "cellar_wine_created" %>
              Ajout&eacute; dans le cellier de
            <% end %>
            <%= link_to event.actor.name, person_url(event.actor) %>, il y a <%= time_ago_in_words event.created_at %>. 
          </small>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
